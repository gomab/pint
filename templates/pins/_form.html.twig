{{ form_start(form) }}
    {{ form_widget(form) }}

<input type="submit" class="btn btn-primary btn-block" value="{{ submitButtonText|default('Create Pin') }}" formnovalidate>
{{ form_end(form) }}

{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}

{#<input type="submit" value="Update Pin" formnovalidate>#}
{#{{ form_end(form) }}#}